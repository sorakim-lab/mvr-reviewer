# mvr-reviewer
A rule-based document (especially Method Validation Report) review system designed for GMP environments where explainability is non-negotiable. Each document type maps to its own rule catalog — no ML, no black box.
